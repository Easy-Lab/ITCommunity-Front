{% extends 'ambassador/profile/layout.html.twig' %}

{% block content %}
    <div class="container mt-2">

        <h2>{{ 'page.ambassador.dashboard.layout.section.navigation.photos'|trans }}</h2>

        {% set element_i = 0 %}
        {% include 'ambassador/_forms/_properties/pictures.html.twig' %}

        <a href="{{ path('ambassador_profile') }}" class="btn btn-primary mt-3" style="float: right;">{{"page.ambassador.pref.mail.goback"|trans }}</a>
    </div>
{% endblock %}
