{% set menu_registration = true %}

{% extends 'base.html.twig' %}

{#{% set step = (app.user is not null ? app.user.step : 0) %}#}

{% block body %}

<div id="register_bg_seb">
{#    {% block layout_header %}#}
{#        {% if app.user is not null %}#}
{#            <div class="container mt-3 register-ambassador-progression">#}
{#                <div class="row mt-4 mb-5">#}
{#                    <div class="col-xs-12 col-4">#}
{#                        <p class="progress-title">{{ ('page.ambassador.register.layout.progress_continue'|trans)}}</p>#}
{#                        {% block register_notice_header %}{% endblock %}#}
{#                    </div>#}
{#                    <div class="col-xs-12 col-8">#}
{#                        {% set progress = ((step == steps) ? '100' : step * 100 / steps) %}#}
{#                        {% if profile_progress is defined %}#}
{#                            {% set progress = profile_progress %}#}
{#                        {% endif %}#}
{#                        <p class="progress-title">{{ ('page.ambassador.register.layout.progress_uncomplete'|trans)}}</p>#}
{#                        <div class="progress-holder">#}
{#                            {% if step > 0 %}#}
{#                                <div class="progress-bar" style="width:{{ progress|round }}% !important">#}
{#                                    <p class="text-right">{{ progress|round }}%</p>#}
{#                                </div>#}
{#                            {% else %}#}
{#                                <p class="text-center">0%</p>#}
{#                            {% endif %}#}
{#                        </div>#}
{#                    </div>#}
{#                </div>#}
{#            </div>#}
{#        {% endif %}#}
{#    {% endblock %}#}


    <div class="container mt-3 register-ambassador">
        <div class="row mt-4 mb-3 register-ambassador-left-column">
            <div class="col-xs-12 col-4 d-none d-md-block">
                <div class="nav flex-column nav-pills" id="navRegister">
                    <a class="nav-link nav-link-uniform-padding {{ actual_route == 'ambassador_register' ? 'active' }}" href="{{ path('ambassador_register') }}">
                        {{ 'page.ambassador.register.layout.steps.step_1'|trans }}
{#                        {% if step > 0 %}<i class="fas fa-check-circle step_confirm"></i>{% endif %}#}
                    </a>
                    <a class="nav-link nav-link-uniform-padding {{ actual_route == 'register_step_2' ? 'active' }}" href="{{ path('register_step_2') }}">
                        {{ 'page.ambassador.register.layout.steps.step_2'|trans }}
                        {#                        {% if step > 0 %}<i class="fas fa-check-circle step_confirm"></i>{% endif %}#}
                    </a>
                    <a class="nav-link nav-link-uniform-padding {{ actual_route == 'register_step_3' ? 'active' }}" href="{{ path('register_step_3') }}">
                        {{ 'page.ambassador.register.layout.steps.step_3'|trans }}
                        {#                        {% if step > 0 %}<i class="fas fa-check-circle step_confirm"></i>{% endif %}#}
                    </a>

                </div>
                <div class="register-notice">
                    {% block register_notice %}{% endblock %}
                </div>
            </div>
            <div class="col-xs-12 col-8 register-ambassador-right-column">
                {% block content %}{% endblock %}
            </div>
        </div>
    </div>
</div>

{% endblock %}
