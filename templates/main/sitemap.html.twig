
{% extends 'base.html.twig' %}

{% block body %}

    <div class="container mt-5 mb-5 sitemap">

        <div class="row">
            <div class="col-md-5">
                <h3>{{ ('page.name_page.sitemap')|trans }}</h3>
                <br>
                <p class="mt-3">
                    <a href="{{ path('home') }}" class="h4">{{ ('page.menu.search')|trans }}</a><br><br>
                    <a href="{{ path('discover') }}" class="h4">{{ ('page.menu.discover')|trans }}</a><br><br>
                    <a href="{{ path('static_page', {'slug': 'personal_data'}) }}"  class="h4 personal_data_plan_du_site">{{ ('page.name_page.personal_data')|trans }}</a><br class="personal_data_plan_du_site"><br class="personal_data_plan_du_site">
                    <a href="{{ path('static_page', {'slug': 'cgu'}) }}" class="h4">{{ ('page.name_page.cgu')|trans }}</a><br><br>
                    <a href="{{ path('static_page', {'slug': 'cookie'}) }}" class="h4">{{ ('page.name_page.cookie')|trans }}</a><br><br>
                    <a href="{{ path('contact_us') }}" class="h4">{{ ('page.name_page.contact')|trans }}</a>
                </p>
            </div>
        </div>

    </div>

{% endblock %}
