<div class="map-result result-profile profile-preview profile-preview-result {{ idx + 1 > 10 ? 'profile-holder-responsive' }}">

    <div class="border_map">
        {% set result = true %}
        <div class="element">
{#            {% set profilePicture = (environment_0 is defined) ? asset(base_path ~ '/uploads/' ~ environment_0['path']) : (environment_1 is defined) ? asset(base_path ~ '/uploads/' ~ environment_1['path']) : (environment_2 is defined) ? asset(base_path ~ '/uploads/' ~ environment_2['path']) : asset(assets_url ~ 'images/img-11.jpg') %}#}
{#            <div class="profile-preview-picture-holder"#}
{#                 style="background-image: url('{{ profilePicture }}'); {{ popup is defined ? 'max-width: 100%; min-width: auto;' }} ">#}
{#                {% if environment_0 is not defined and environment_1 is not defined and environment_2 is not defined %}#}
{#                    <div class="user-picture-missing">#}
{#                        <div class="missing-message">{{ ('page._common.global.missing_image_placeholder'|trans) |raw }}</div>#}
{#                    </div>#}
{#                {% endif %}#}
{#            </div>#}
            <p class="city_map_break">
                {{ user['zipcode'] }} - {{ user['city'] }}
            </p>
            <h6 class="colorPseudo">{{ user['username'] }}</h6>
            <p style="font-size: 10px; margin-top:8px;" class="user_rating_resp">
                {% set rating = user['reviews'][0]['rating'] %}

                <span class="rating">
                    {{ user['reviews'][0]['companyComponent'] }} {{ user['reviews'][0]['nameComponent'] }} :
        {% for i in 1..5 %}
            <span class="icon-star{{ rating < i ? '-empty' : '' }}"></span>
        {% endfor %}
    </span>
            </p>
            <span class="block-ellipsis">
        {{ user['reviews'][0]['body'] }}
    </span>
            <p style="font-size: 10px; margin-top:8px;" class="user_rating_resp">
                {% set rating2 = user['reviews'][1]['rating'] %}

                <span class="rating">
                    {{ user['reviews'][1]['companyComponent'] }} {{ user['reviews'][1]['nameComponent'] }} :
        {% for i in 1..5 %}
            <span class="icon-star{{ rating2 < i ? '-empty' : '' }}"></span>
        {% endfor %}
    </span>
            </p>
            <span class="block-ellipsis">
        {{ user['reviews'][1]['body'] }}
    </span>
            <a href="" class="btn btn-theme profile-preview-action" target="_blank" id="buttonViewProfile">
                {{ ('page.ambassador.dashboard.layout.section.profile.view_profil') |trans }} <i class="fa fa-angle-right float-right p-1"></i>
            </a>

        </div>

    </div>
</div>
